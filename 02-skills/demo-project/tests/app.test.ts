import { describe, it, expect } from 'vitest';

describe('User API', () => {
  it('should exist', () => {
    expect(true).toBe(true);
  });

  // Intentionally sparse tests for the test-reviewing skill exercise
  // Students will build a "test-reviewing" skill that identifies gaps
});
